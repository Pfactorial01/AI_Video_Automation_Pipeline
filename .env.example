# Copy to .env and fill in (never commit .env).

# --- Phase 1: OpenAI (script + structured scenes) ---
OPENAI_API_KEY=
# Optional: model that supports structured outputs (json_schema)
# OPENAI_SCRIPT_MODEL=gpt-4o-mini

# --- n8n / HTTP nodes: mock vendors (docker compose mock-api) ---
MOCK_VENDOR_BASE_URL=http://127.0.0.1:8080

# --- Production vendors (add when integrating) ---
# GENAIPRO_API_KEY=
# RUNWAY_API_KEY=
# SUNO_API_KEY=
# CREATOMATE_API_KEY=
# RUNPOD_API_KEY=

# --- Google OAuth (n8n credentials; not always needed in .env) ---
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
